# 🧩 Thiết kế lớp
I. Các lớp phân tích từ use case :
1. User:
  - Properties: tên, email, mật khẩu, thông tin tài khoản GG/ FB ?
  - Method: Đăng ký/ Đăng nhập/ Đăng xuất/ Quản lý tài khoản

2. MediaItem: đại diện cho các phương tiện ảnh/ gif/ video
- Properties: đường dẫn tới tệp, thời gian, mô tả,...
- Method:

3. Album:
- Properties: tên, mô tả, danh sách ảnh...
- Method:

4. PhotoEditor: quản lý công cụ chỉnh sửa ảnh và bộ lọc
- Properties:
- Method: cắt/ xoay/ hiệu ứng...

5. PhotoUploader: quản lý quá trình tải ảnh lên từ thiết bị hoặc máy ảnh của người dùng
- Properties:
- Method: tải ảnh lên từ thiết bị/ máy ảnh của user

6. MediaStorage: quản lý lưu trữ và quản lý các phương tiện, tổ chức vào các album
- Properties:
- Method: tổ chức vào album, quản lý lưu trữ

7. SearchEngine: quản lý chức năng tìm kiếm ảnh theo tên, loại extension của ảnh, kích thước...
- Properties:
- Method: tìm kiếm theo tên, loại extension, kích thước, location,...

8. SecurityManager: quản lý bảo mật và quyền truy cập, xác thực và quản lý truy cập đối với album
- Properties:
- Method: xác thực/ quản lý truy cập ?

9. DataSyncService: quản lý đồng bộ hóa dữ liệu với dịch vụ trực tuyến như Google Drive
- Properties:
- Method: đồng bộ hóa 

10. NotificationManager: quản lý thông báo và nhắc nhở về kỉ niệm, ảnh liên quan...
- Properties:
- Method: thông báo, nhắc kỉ niệm..

11. ImageDuplicationChecker: kiểm tra và quản lý ảnh trùng lặp ...
- Properties:
- Method: kiểm tra ảnh trùng 

12. ImageTransformer: chuyển đổi ảnh thành văn bản,
- Properties:
- Method: chuyển ảnh thành văn bản

___

// phần của như ý 

Picture: + picture
	 + information picture 
		. name
		. tag
		. size
		. date create
		. location
		. security(true/false)
		. detail security
		. before folder 
		. current folder
		. note
user:  	+ user ID
	+ user name
	+ password
	+ finger print
	+ email

folder/album: 	+ tag
		+ name
		+ total size
		+ date create

Main layout 
	+ folder default: bin, favorite(empty)
	+ function and icon

1. đăng nhập đăng ký
 1.1 Đăng nhập
  1.1.1đăng nhập thông thường
	+ get Name User
	+ get password
	+ check infomation in user database
	+ move to main layout
  1.1.2 đăng nhập với facebook
	+ login facebook
	+ get access token
	+ check infomation name in user database		
	+ move to main layout
  1.1.3 Đăng nhập với google 
	+ login google account
	+ get access token
	+ check infomation name in user database
	+ move to main layout
 1.1.4 Đăng nhập với vân tay 
	+ get finger print
	+ check infomation finger print in user database
	+ move to main layout
 1.2 Đăng ký
  1.2.1 Đăng ký thông thường
	+ Create new user(set userID, get userName, get userEmail, get userPassword, get userFingerPrint) 
	+ Add new user	
	+ move to login layout(empty)
  1.2.2 Đăng ký qua facebook 
	+ login facebook
	+ get access token
	+ Create new user(userID = random, get userName,userPassword = NULL, userFinger= NULL, userEmail = NULL)
	+ add new user 
	+ move to main layout(empty)
  1.2.3 Đăng ký qua google 
	+ login google account
	+ get access token
	+ Create new user(userID =random, get userName, get userEmail, userPassword = NULL, userFinger = NULL)
	+ add new user 
	+ move to main layout(empty(
2. tạo câu chuyện 
	+ get picture 
	+ get sound
	+ Create story
	
3. Quản lý album
 3.1 Tạo album
	+ Create new album(nameAlbum)
	+ add to database
 3.2 Chỉnh sửa/xóa album
	+ get album
	+ update album
4. Sắp xếp ảnh 
 4.1 sắp xếp theo tên 
	+ get picture name
	+ sort(name)
	+ display 	
 4.2 sắp xếp theo ngày 
	+ get date create
	+ sort(date)
	+ display
 4.3 sắp xếp theo location 
	+ get location
	+ sort(location)
	+ display
5. Thêm ảnh 
	+ Create picture infomation(name, source, security)
	+ set tag 
	+ set current folder (facebook, zalo is default with the source image from facebook and zalo)
	+ set date create
	+ set size (from source)
	+ set location
	+ update picture to data base
	+ update folder size
	
6. chỉnh sửa và bộ lọc 
	+ get picture	
	+ get infomation picture
	+ copy piture into a temporary
	+ edit picture
	+ update infomation(name, size, location, tag)
	+ save and update to database
7. xem và hiển thị ảnh
	+ get picturde
	+ rotate
	+ zoom in
	+zoom out
8. Tải ảnh qua QR code 
	+ get picture 
	+ create QR code 
	+ share QR code to messs/zalo/insta....
9. tìm kiếm hình ảnh 
	+ get keyword
	+ search picture base keyword(name,tag,date, location)
	+ display 
10. chia sẻ ảnh 
	+ get picture
	+ share picture 
11. xóa ảnh 
 11.1 Xóa ảnh trên thư mục gốc
	+ get picture
	+ set before folder( = current folder)
	+ set current folder( = bin)
	+ update picture infomation
 11.2 xóa ảnh trong thư mục bin
	+ get picture 
	+ delete picure in database 
 11.3 khôi phục ảnh đã xóa 
	+ get picture 
	+ set current folder ( = before folder)
	+ set before folder (= NULL)
12. bảo mật và quyền truy cập 
	+ set security (=true)
	+ set detail security
13. đồng bộ hóa và sao lưu 
	...
14. ghi chú ảnh 
	+ get picture 
	+ set note
	
15. Chuyển đổi sang text
	+ get picture
 	+ convert to text
16. đặt làm ảnh nền, ảnh khóa 
	+ get picute 
	+ connect to device 
	+ set picture as wallpaper 
17. Thống kê 
	+ loop for all folder and sum the size of each folder 
	+ count the number of picture in database
18. Gợi nhớ 
	+ get current date(date/month)
	+ search in database and find picture having create date match with current date
	+ display 
19. lọc ảnh trùng 
	+ search in database pictures with the same 
	+ display all of them 
20. xem ảnh 
	+ get all piture(in paticular folder)
	+ display with grid view 
	+ display with detail view 
___

II. Xếp loại các chức năng


___

III. Thể hiện mối kết hợp thông qua UML


___

IV. Chuyển đổi lớp sang bảng dữ liệu/ Chuẩn hóa lược đồ


___

V. Luồng xử lý các chức năng
1. 
